import { Cloud } from 'lucide-react';

export const kubernetesCheatsheet = {
  id: 'kubernetes',
  name: 'Kubernetes Commands',
  description: 'Kubernetes (kubectl) commands and operations (v1.31+)',
  icon: Cloud,
  colorTheme: 'blue' as const,
  sections: [
    // BEGINNER LEVEL
    {
      title: 'Getting Started with Kubernetes',
      commands: [
        {
          command: 'What is Kubernetes',
          description: 'Understanding Kubernetes fundamentals',
          usage: 'Kubernetes basics overview',
          example: '# Kubernetes is a container orchestration platform\n# Automates deployment, scaling, and management\n# Key concepts: Pods, Services, Deployments, Namespaces\n# Master components: API Server, etcd, Scheduler, Controller\n# Node components: kubelet, kube-proxy, container runtime',
        },
        {
          command: 'kubectl version',
          description: 'Show Kubernetes client and server version',
          usage: 'kubectl version [flags]',
          example: 'kubectl version\nkubectl version --short\nkubectl version --client\nkubectl version --output=yaml\nkubectl version --output=json',
        },
        {
          command: 'kubectl cluster-info',
          description: 'Display cluster endpoint information',
          usage: 'kubectl cluster-info [flags]',
          example: 'kubectl cluster-info\nkubectl cluster-info dump\nkubectl cluster-info dump --namespaces=default\nkubectl cluster-info dump --output-directory=/tmp/cluster-dump',
        },
        {
          command: 'kubectl config help',
          description: 'Get help with kubectl configuration',
          usage: 'kubectl config --help',
          example: 'kubectl config --help\nkubectl config view --help\nkubectl config get-contexts --help',
        },
        {
          command: 'Basic kubectl syntax',
          description: 'Understanding kubectl command structure',
          usage: 'kubectl [command] [TYPE] [NAME] [flags]',
          example: '# Basic structure\nkubectl <action> <resource-type> <resource-name> <options>\n# Examples:\nkubectl get pods\nkubectl describe pod my-pod\nkubectl delete deployment nginx',
        },
      ],
    },
    {
      title: 'Configuration and Context Management',
      commands: [
        {
          command: 'kubectl config view',
          description: 'Display merged kubeconfig settings',
          usage: 'kubectl config view [flags]',
          example: 'kubectl config view\nkubectl config view --minify\nkubectl config view --raw\nkubectl config view -o jsonpath=\'{.users[*].name}\'\nkubectl config view -o yaml',
        },
        {
          command: 'kubectl config get-contexts',
          description: 'List all available contexts',
          usage: 'kubectl config get-contexts [flags]',
          example: 'kubectl config get-contexts\nkubectl config get-contexts -o name\nkubectl config get-contexts --no-headers',
        },
        {
          command: 'kubectl config current-context',
          description: 'Show the current context',
          usage: 'kubectl config current-context',
          example: 'kubectl config current-context',
        },
        {
          command: 'kubectl config use-context',
          description: 'Switch to a different context',
          usage: 'kubectl config use-context <context-name>',
          example: 'kubectl config use-context minikube\nkubectl config use-context prod-cluster\nkubectl config use-context dev-cluster',
        },
        {
          command: 'kubectl config set-context',
          description: 'Set context entry in kubeconfig',
          usage: 'kubectl config set-context <context-name> [flags]',
          example: 'kubectl config set-context --current --namespace=dev\nkubectl config set-context my-context --cluster=my-cluster --user=my-user --namespace=prod\nkubectl config set-context dev-context --cluster=dev-cluster --user=dev-user --namespace=development',
        },
        {
          command: 'kubectl config set-cluster',
          description: 'Set cluster entry in kubeconfig',
          usage: 'kubectl config set-cluster <cluster-name> [flags]',
          example: 'kubectl config set-cluster dev-cluster --server=https://dev.example.com\nkubectl config set-cluster prod-cluster --server=https://prod.example.com --certificate-authority=/path/to/ca',
        },
        {
          command: 'kubectl config set-credentials',
          description: 'Set user credentials in kubeconfig',
          usage: 'kubectl config set-credentials <name> [flags]',
          example: 'kubectl config set-credentials my-user --token=<token>\nkubectl config set-credentials admin --client-certificate=cert.pem --client-key=key.pem\nkubectl config set-credentials dev-user --username=dev --password=secret',
        },
      ],
    },
    {
      title: 'Basic Pod Operations',
      commands: [
        {
          command: 'kubectl get pods',
          description: 'List pods in current or all namespaces',
          usage: 'kubectl get pods [flags]',
          example: 'kubectl get pods\nkubectl get pods -A # All namespaces\nkubectl get pods -n <namespace>\nkubectl get pods -o wide\nkubectl get pods --show-labels\nkubectl get pods --field-selector=status.phase=Running\nkubectl get pods --selector=app=nginx\nkubectl get pods -l version=v1.0',
        },
        {
          command: 'kubectl describe pod',
          description: 'Show detailed information about a pod',
          usage: 'kubectl describe pod <pod-name> [flags]',
          example: 'kubectl describe pod my-pod\nkubectl describe pod my-pod -n <namespace>\nkubectl describe pods # All pods in namespace\nkubectl describe pod my-pod --show-events',
        },
        {
          command: 'kubectl run',
          description: 'Create and run a pod with specified image',
          usage: 'kubectl run <name> --image=<image> [flags]',
          example: 'kubectl run nginx --image=nginx\nkubectl run nginx --image=nginx:1.25 --port=80\nkubectl run test --image=busybox -it --rm -- sh\nkubectl run nginx --image=nginx --dry-run=client -o yaml > pod.yaml\nkubectl run debug --image=alpine -it --rm -- /bin/sh\nkubectl run app --image=node:18 --restart=Always',
        },
        {
          command: 'kubectl delete pod',
          description: 'Delete one or more pods',
          usage: 'kubectl delete pod <pod-name> [flags]',
          example: 'kubectl delete pod my-pod\nkubectl delete pod my-pod --grace-period=0 --force\nkubectl delete pods --all\nkubectl delete pods -l app=nginx\nkubectl delete pods --field-selector=status.phase=Failed\nkubectl delete pod my-pod -n production',
        },
        {
          command: 'kubectl logs',
          description: 'Print logs from containers in a pod',
          usage: 'kubectl logs <pod-name> [flags]',
          example: 'kubectl logs my-pod\nkubectl logs my-pod -c <container>\nkubectl logs my-pod --previous\nkubectl logs -f my-pod # Follow logs\nkubectl logs my-pod --tail=50\nkubectl logs my-pod --since=1h\nkubectl logs -l app=nginx --all-containers=true\nkubectl logs my-pod --timestamps=true',
        },
        {
          command: 'kubectl exec',
          description: 'Execute command in a container',
          usage: 'kubectl exec <pod-name> [flags] -- <command>',
          example: 'kubectl exec my-pod -- ls /\nkubectl exec -it my-pod -- /bin/bash\nkubectl exec my-pod -c <container> -- env\nkubectl exec -it my-pod -- sh\nkubectl exec my-pod -- printenv | grep NODE\nkubectl exec -it my-pod -c nginx -- nginx -t',
        },
        {
          command: 'kubectl port-forward',
          description: 'Forward local port to a port in the pod',
          usage: 'kubectl port-forward <pod-name> <local-port>:<pod-port>',
          example: 'kubectl port-forward my-pod 8080:80\nkubectl port-forward my-pod :80 # Random local port\nkubectl port-forward pods/my-pod 8080:80\nkubectl port-forward --address 0.0.0.0 my-pod 8080:80\nkubectl port-forward my-pod 8000:80 9000:9090',
        },
      ],
    },

    // INTERMEDIATE LEVEL
    {
      title: 'Deployment Management',
      commands: [
        {
          command: 'kubectl create deployment',
          description: 'Create a new deployment',
          usage: 'kubectl create deployment <name> [flags]',
          example: 'kubectl create deployment nginx --image=nginx\nkubectl create deployment nginx --image=nginx:1.25 --replicas=3\nkubectl create deployment nginx --image=nginx --dry-run=client -o yaml > deployment.yaml\nkubectl create deployment webapp --image=nginx --port=80',
        },
        {
          command: 'kubectl get deployments',
          description: 'List deployments in the namespace',
          usage: 'kubectl get deployments [flags]',
          example: 'kubectl get deployments\nkubectl get deploy # Short form\nkubectl get deployments -A\nkubectl get deployments -o wide\nkubectl get deployments --show-labels\nkubectl get deploy -l app=nginx',
        },
        {
          command: 'kubectl describe deployment',
          description: 'Show detailed deployment information',
          usage: 'kubectl describe deployment <name>',
          example: 'kubectl describe deployment nginx\nkubectl describe deploy nginx\nkubectl describe deployment webapp -n production',
        },
        {
          command: 'kubectl scale deployment',
          description: 'Scale a deployment to specified number of replicas',
          usage: 'kubectl scale deployment <name> --replicas=<count>',
          example: 'kubectl scale deployment nginx --replicas=5\nkubectl scale deploy nginx --replicas=3\nkubectl scale deployment nginx --current-replicas=2 --replicas=3\nkubectl scale deployment --all --replicas=2',
        },
        {
          command: 'kubectl edit deployment',
          description: 'Edit a deployment resource',
          usage: 'kubectl edit deployment <name>',
          example: 'kubectl edit deployment nginx\nkubectl edit deploy nginx\nkubectl edit deployment webapp -n dev',
        },
        {
          command: 'kubectl rollout status',
          description: 'Check the status of a deployment rollout',
          usage: 'kubectl rollout status deployment/<name>',
          example: 'kubectl rollout status deployment/nginx\nkubectl rollout status deploy/nginx --timeout=60s\nkubectl rollout status deployment/webapp -n production',
        },
        {
          command: 'kubectl rollout history',
          description: 'View rollout history of a deployment',
          usage: 'kubectl rollout history deployment/<name>',
          example: 'kubectl rollout history deployment/nginx\nkubectl rollout history deployment/nginx --revision=2\nkubectl rollout history deployment/webapp -n production',
        },
        {
          command: 'kubectl rollout undo',
          description: 'Rollback a deployment to previous revision',
          usage: 'kubectl rollout undo deployment/<name>',
          example: 'kubectl rollout undo deployment/nginx\nkubectl rollout undo deployment/nginx --to-revision=2\nkubectl rollout undo deployment/webapp -n production',
        },
        {
          command: 'kubectl rollout restart',
          description: 'Restart a deployment',
          usage: 'kubectl rollout restart deployment/<name>',
          example: 'kubectl rollout restart deployment/nginx\nkubectl rollout restart deploy/nginx\nkubectl rollout restart deployment/webapp -n production',
        },
        {
          command: 'kubectl rollout pause',
          description: 'Pause a deployment rollout',
          usage: 'kubectl rollout pause deployment/<name>',
          example: 'kubectl rollout pause deployment/nginx\nkubectl rollout pause deployment/webapp -n production',
        },
        {
          command: 'kubectl rollout resume',
          description: 'Resume a paused deployment rollout',
          usage: 'kubectl rollout resume deployment/<name>',
          example: 'kubectl rollout resume deployment/nginx\nkubectl rollout resume deployment/webapp -n production',
        },
        {
          command: 'kubectl set image',
          description: 'Update container image in a deployment',
          usage: 'kubectl set image deployment/<name> <container>=<image>',
          example: 'kubectl set image deployment/nginx nginx=nginx:1.26\nkubectl set image deployment/nginx *=nginx:1.26 # All containers\nkubectl set image deployment/webapp app=node:18 -n production',
        },
        {
          command: 'kubectl autoscale',
          description: 'Create an autoscaler for a deployment',
          usage: 'kubectl autoscale deployment <name> [flags]',
          example: 'kubectl autoscale deployment nginx --min=2 --max=10 --cpu-percent=80\nkubectl autoscale deploy nginx --min=3 --max=15 --cpu-percent=70\nkubectl autoscale deployment webapp --min=1 --max=5 --cpu-percent=60 -n production',
        },
      ],
    },
    {
      title: 'Service Management',
      commands: [
        {
          command: 'kubectl get services',
          description: 'List services in the namespace',
          usage: 'kubectl get services [flags]',
          example: 'kubectl get services\nkubectl get svc # Short form\nkubectl get services -A\nkubectl get svc -o wide\nkubectl get svc --show-labels\nkubectl get svc -l app=nginx',
        },
        {
          command: 'kubectl describe service',
          description: 'Show detailed service information',
          usage: 'kubectl describe service <name>',
          example: 'kubectl describe service nginx\nkubectl describe svc nginx\nkubectl describe service webapp -n production',
        },
        {
          command: 'kubectl expose',
          description: 'Expose a resource as a service',
          usage: 'kubectl expose <resource> <name> [flags]',
          example: 'kubectl expose deployment nginx --port=80 --type=LoadBalancer\nkubectl expose pod nginx --port=80 --target-port=8000\nkubectl expose deployment nginx --port=80 --type=NodePort\nkubectl expose deploy nginx --port=80 --name=nginx-svc\nkubectl expose deployment webapp --port=3000 --target-port=3000 --type=ClusterIP',
        },
        {
          command: 'kubectl create service',
          description: 'Create a service using subcommands',
          usage: 'kubectl create service <type> <name> [flags]',
          example: 'kubectl create service clusterip nginx --tcp=80:80\nkubectl create service nodeport nginx --tcp=80:80\nkubectl create service loadbalancer nginx --tcp=80:80\nkubectl create service clusterip webapp --tcp=3000:3000 -n production',
        },
        {
          command: 'kubectl delete service',
          description: 'Delete a service',
          usage: 'kubectl delete service <name>',
          example: 'kubectl delete service nginx\nkubectl delete svc nginx\nkubectl delete service webapp -n production\nkubectl delete services -l app=nginx',
        },
        {
          command: 'kubectl port-forward service',
          description: 'Forward local port to a service',
          usage: 'kubectl port-forward service/<name> <local>:<remote>',
          example: 'kubectl port-forward service/nginx 8080:80\nkubectl port-forward svc/nginx 8080:80\nkubectl port-forward service/webapp 3000:3000 -n production',
        },
        {
          command: 'Service Types',
          description: 'Understanding different service types',
          usage: 'Service type selection',
          example: '# ClusterIP (default) - Internal cluster access\nkubectl expose deployment nginx --port=80 --type=ClusterIP\n\n# NodePort - External access via node port\nkubectl expose deployment nginx --port=80 --type=NodePort\n\n# LoadBalancer - External load balancer\nkubectl expose deployment nginx --port=80 --type=LoadBalancer\n\n# ExternalName - DNS alias\nkubectl create service externalname my-service --external-name my.database.com',
        },
      ],
    },
    {
      title: 'Namespace Management',
      commands: [
        {
          command: 'kubectl get namespaces',
          description: 'List all namespaces',
          usage: 'kubectl get namespaces [flags]',
          example: 'kubectl get namespaces\nkubectl get ns # Short form\nkubectl get ns --show-labels\nkubectl get ns -o wide',
        },
        {
          command: 'kubectl create namespace',
          description: 'Create a new namespace',
          usage: 'kubectl create namespace <name>',
          example: 'kubectl create namespace dev\nkubectl create ns production\nkubectl create namespace testing\nkubectl create namespace staging --dry-run=client -o yaml',
        },
        {
          command: 'kubectl describe namespace',
          description: 'Show namespace details',
          usage: 'kubectl describe namespace <name>',
          example: 'kubectl describe namespace dev\nkubectl describe ns dev\nkubectl describe namespace production --show-events',
        },
        {
          command: 'kubectl delete namespace',
          description: 'Delete a namespace and all its resources',
          usage: 'kubectl delete namespace <name>',
          example: 'kubectl delete namespace dev\nkubectl delete ns dev\nkubectl delete namespace testing --force --grace-period=0',
        },
        {
          command: 'kubectl config set-context namespace',
          description: 'Set default namespace for context',
          usage: 'kubectl config set-context --current --namespace=<name>',
          example: 'kubectl config set-context --current --namespace=dev\nkubectl config set-context --current --namespace=production\nkubectl config set-context prod-context --namespace=production',
        },
        {
          command: 'Resource isolation',
          description: 'Using namespaces for resource isolation',
          usage: 'Namespace-based isolation',
          example: '# Create namespace with resource quotas\nkubectl create namespace dev\nkubectl apply -f resource-quota.yaml -n dev\n\n# Set default namespace\nkubectl config set-context --current --namespace=dev\n\n# All commands now target dev namespace\nkubectl get pods\nkubectl get services',
        },
      ],
    },
    {
      title: 'ConfigMaps and Secrets',
      commands: [
        {
          command: 'kubectl get configmaps',
          description: 'List ConfigMaps in namespace',
          usage: 'kubectl get configmaps [flags]',
          example: 'kubectl get configmaps\nkubectl get cm # Short form\nkubectl get cm -A\nkubectl get cm -o yaml\nkubectl get cm -l app=nginx',
        },
        {
          command: 'kubectl create configmap',
          description: 'Create a ConfigMap from various sources',
          usage: 'kubectl create configmap <name> [flags]',
          example: 'kubectl create configmap my-config --from-literal=key=value\nkubectl create cm my-config --from-file=config.txt\nkubectl create cm my-config --from-file=configs/\nkubectl create cm my-config --from-env-file=.env\nkubectl create configmap app-config --from-literal=DATABASE_URL=localhost --from-literal=DEBUG=true',
        },
        {
          command: 'kubectl describe configmap',
          description: 'Show ConfigMap details',
          usage: 'kubectl describe configmap <name>',
          example: 'kubectl describe configmap my-config\nkubectl describe cm my-config\nkubectl describe configmap app-config -n production',
        },
        {
          command: 'kubectl edit configmap',
          description: 'Edit a ConfigMap',
          usage: 'kubectl edit configmap <name>',
          example: 'kubectl edit configmap my-config\nkubectl edit cm app-config -n production',
        },
        {
          command: 'kubectl delete configmap',
          description: 'Delete a ConfigMap',
          usage: 'kubectl delete configmap <name>',
          example: 'kubectl delete configmap my-config\nkubectl delete cm my-config\nkubectl delete configmap app-config -n production',
        },
        {
          command: 'kubectl get secrets',
          description: 'List secrets in namespace',
          usage: 'kubectl get secrets [flags]',
          example: 'kubectl get secrets\nkubectl get secret # Singular form\nkubectl get secrets -A\nkubectl get secrets -o yaml\nkubectl get secrets -l app=nginx',
        },
        {
          command: 'kubectl create secret',
          description: 'Create secrets of different types',
          usage: 'kubectl create secret <type> <name> [flags]',
          example: 'kubectl create secret generic my-secret --from-literal=password=secret123\nkubectl create secret generic my-secret --from-file=ssh-key=~/.ssh/id_rsa\nkubectl create secret docker-registry regcred --docker-server=<server> --docker-username=<user> --docker-password=<password>\nkubectl create secret tls tls-secret --cert=path/to/cert --key=path/to/key',
        },
        {
          command: 'kubectl describe secret',
          description: 'Show secret details (values hidden)',
          usage: 'kubectl describe secret <name>',
          example: 'kubectl describe secret my-secret\nkubectl describe secret regcred\nkubectl describe secret tls-secret -n production',
        },
        {
          command: 'kubectl get secret (decode)',
          description: 'Get and decode secret values',
          usage: 'kubectl get secret <name> -o jsonpath=\'{.data.*}\' | base64 -d',
          example: 'kubectl get secret my-secret -o yaml\nkubectl get secret my-secret -o jsonpath=\'{.data.password}\' | base64 -d\nkubectl get secret my-secret -o json | jq -r \'.data | map_values(@base64d)\'\nkubectl get secret regcred -o jsonpath=\'{.data..docker-password}\' | base64 -d',
        },
        {
          command: 'kubectl delete secret',
          description: 'Delete a secret',
          usage: 'kubectl delete secret <name>',
          example: 'kubectl delete secret my-secret\nkubectl delete secret regcred\nkubectl delete secret tls-secret -n production',
        },
      ],
    },

    // ADVANCED LEVEL
    {
      title: 'Persistent Volumes and Storage',
      commands: [
        {
          command: 'kubectl get pv',
          description: 'List PersistentVolumes in cluster',
          usage: 'kubectl get pv [flags]',
          example: 'kubectl get pv\nkubectl get persistentvolumes\nkubectl get pv -o wide\nkubectl get pv --show-labels\nkubectl get pv -l storage=ssd',
        },
        {
          command: 'kubectl get pvc',
          description: 'List PersistentVolumeClaims in namespace',
          usage: 'kubectl get pvc [flags]',
          example: 'kubectl get pvc\nkubectl get persistentvolumeclaims\nkubectl get pvc -A\nkubectl get pvc -n <namespace>\nkubectl get pvc -o wide\nkubectl get pvc --show-labels',
        },
        {
          command: 'kubectl describe pv',
          description: 'Show PersistentVolume details',
          usage: 'kubectl describe pv <name>',
          example: 'kubectl describe pv my-pv\nkubectl describe persistentvolume my-pv\nkubectl describe pv pv-001 --show-events',
        },
        {
          command: 'kubectl describe pvc',
          description: 'Show PersistentVolumeClaim details',
          usage: 'kubectl describe pvc <name>',
          example: 'kubectl describe pvc my-pvc\nkubectl describe persistentvolumeclaim my-pvc\nkubectl describe pvc app-data -n production --show-events',
        },
        {
          command: 'kubectl delete pvc',
          description: 'Delete a PersistentVolumeClaim',
          usage: 'kubectl delete pvc <name>',
          example: 'kubectl delete pvc my-pvc\nkubectl delete pvc --all\nkubectl delete pvc app-data -n production\nkubectl delete pvc -l app=nginx',
        },
        {
          command: 'kubectl get storageclass',
          description: 'List StorageClasses in cluster',
          usage: 'kubectl get storageclass [flags]',
          example: 'kubectl get storageclass\nkubectl get sc # Short form\nkubectl get sc -o wide\nkubectl get sc --show-labels\nkubectl get sc -l type=ssd',
        },
        {
          command: 'kubectl describe storageclass',
          description: 'Show StorageClass details',
          usage: 'kubectl describe storageclass <name>',
          example: 'kubectl describe storageclass fast-ssd\nkubectl describe sc fast-ssd\nkubectl describe storageclass standard --show-events',
        },
        {
          command: 'Volume management',
          description: 'Understanding volume types and usage',
          usage: 'Volume types and selection',
          example: '# Different volume types\n# hostPath - Node local storage\n# emptyDir - Ephemeral pod storage\n# persistentVolumeClaim - Persistent storage\n# configMap/secret - Configuration data\n# downwardAPI - Pod metadata\n\n# Create PVC with specific storage class\nkubectl apply -f - <<EOF\napiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: app-pvc\nspec:\n  accessModes:\n    - ReadWriteOnce\n  storageClassName: fast-ssd\n  resources:\n    requests:\n      storage: 10Gi\nEOF',
        },
      ],
    },
    {
      title: 'Node Management and Maintenance',
      commands: [
        {
          command: 'kubectl get nodes',
          description: 'List all cluster nodes',
          usage: 'kubectl get nodes [flags]',
          example: 'kubectl get nodes\nkubectl get nodes -o wide\nkubectl get nodes --show-labels\nkubectl get nodes --selector=node-role.kubernetes.io/control-plane\nkubectl get nodes -l environment=production\nkubectl get nodes --sort-by=.metadata.name',
        },
        {
          command: 'kubectl describe node',
          description: 'Show detailed node information',
          usage: 'kubectl describe node <name>',
          example: 'kubectl describe node worker-1\nkubectl describe nodes # All nodes\nkubectl describe node control-plane-1 --show-events\nkubectl describe node worker-2 -o wide',
        },
        {
          command: 'kubectl top node',
          description: 'Display resource usage of nodes',
          usage: 'kubectl top node [flags]',
          example: 'kubectl top node\nkubectl top node worker-1\nkubectl top nodes --sort-by=cpu\nkubectl top nodes --sort-by=memory\nkubectl top node --no-headers',
        },
        {
          command: 'kubectl cordon',
          description: 'Mark node as unschedulable',
          usage: 'kubectl cordon <node>',
          example: 'kubectl cordon worker-1\nkubectl cordon worker-1 --dry-run\nkubectl cordon worker-1 --ignore-daemonsets',
        },
        {
          command: 'kubectl uncordon',
          description: 'Mark node as schedulable',
          usage: 'kubectl uncordon <node>',
          example: 'kubectl uncordon worker-1\nkubectl uncordon worker-1 --dry-run\nkubectl uncordon worker-1 --ignore-daemonsets',
        },
        {
          command: 'kubectl drain',
          description: 'Drain node for maintenance',
          usage: 'kubectl drain <node> [flags]',
          example: 'kubectl drain worker-1 --ignore-daemonsets\nkubectl drain worker-1 --force --delete-emptydir-data\nkubectl drain worker-1 --grace-period=300\nkubectl drain worker-1 --delete-local-data\nkubectl drain worker-1 --ignore-daemonsets --delete-emptydir-data --force',
        },
        {
          command: 'kubectl taint',
          description: 'Add or remove node taints',
          usage: 'kubectl taint nodes <node> <key>=<value>:<effect>',
          example: 'kubectl taint nodes worker-1 key=value:NoSchedule\nkubectl taint nodes worker-1 key=value:NoSchedule- # Remove taint\nkubectl taint nodes worker-1 dedicated=gpu:NoSchedule\nkubectl taint nodes control-plane-1 node-role.kubernetes.io/control-plane:NoSchedule-\nkubectl taint node worker-1 app=database:NoExecute',
        },
        {
          command: 'kubectl label node',
          description: 'Add or update node labels',
          usage: 'kubectl label node <name> <key>=<value>',
          example: 'kubectl label node worker-1 disktype=ssd\nkubectl label node worker-1 env=production\nkubectl label node worker-1 disktype- # Remove label\nkubectl label nodes --all status=verified\nkubectl label node worker-1 zone=us-west1-a --overwrite',
        },
      ],
    },
    {
      title: 'StatefulSets and DaemonSets',
      commands: [
        {
          command: 'kubectl get statefulsets',
          description: 'List StatefulSets in namespace',
          usage: 'kubectl get statefulsets [flags]',
          example: 'kubectl get statefulsets\nkubectl get sts # Short form\nkubectl get sts -A\nkubectl get sts -o wide\nkubectl get sts --show-labels\nkubectl get sts -l app=database',
        },
        {
          command: 'kubectl create statefulset',
          description: 'Create a StatefulSet',
          usage: 'kubectl create statefulset <name> [flags]',
          example: 'kubectl create statefulset mongodb --image=mongo:5.0\nkubectl create statefulset redis --image=redis:7 --replicas=3\nkubectl create statefulset webapp --image=nginx --port=80\nkubectl create statefulset database --image=postgres:13 --dry-run=client -o yaml',
        },
        {
          command: 'kubectl describe statefulset',
          description: 'Show StatefulSet details',
          usage: 'kubectl describe statefulset <name>',
          example: 'kubectl describe statefulset mongodb\nkubectl describe sts mongodb\nkubectl describe statefulset redis -n production --show-events',
        },
        {
          command: 'kubectl scale statefulset',
          description: 'Scale a StatefulSet',
          usage: 'kubectl scale statefulset <name> --replicas=<count>',
          example: 'kubectl scale statefulset mongodb --replicas=5\nkubectl scale sts redis --replicas=3\nkubectl scale statefulset webapp --current-replicas=2 --replicas=4',
        },
        {
          command: 'kubectl get daemonsets',
          description: 'List DaemonSets in namespace',
          usage: 'kubectl get daemonsets [flags]',
          example: 'kubectl get daemonsets\nkubectl get ds # Short form\nkubectl get ds -A\nkubectl get ds -o wide\nkubectl get ds --show-labels\nkubectl get ds -l app=monitoring',
        },
        {
          command: 'kubectl create daemonset',
          description: 'Create a DaemonSet',
          usage: 'kubectl create daemonset <name> [flags]',
          example: 'kubectl create daemonset fluentd --image=fluent/fluentd:v1.16\nkubectl create daemonset node-exporter --image=prom/node-exporter\nkubectl create daemonset log-collector --image=logstash:7.15\nkubectl create daemonset monitoring --image=prom/node-exporter --dry-run=client -o yaml',
        },
        {
          command: 'kubectl describe daemonset',
          description: 'Show DaemonSet details',
          usage: 'kubectl describe daemonset <name>',
          example: 'kubectl describe daemonset fluentd\nkubectl describe ds fluentd\nkubectl describe daemonset node-exporter -n monitoring --show-events',
        },
        {
          command: 'StatefulSet vs Deployment',
          description: 'Understanding when to use StatefulSets',
          usage: 'StatefulSet use cases',
          example: '# Use StatefulSets for:\n# - Databases (MySQL, PostgreSQL, MongoDB)\n# - Distributed systems (Zookeeper, etcd)\n# - Applications requiring stable network identifiers\n# - Ordered deployment and scaling\n# - Persistent storage with unique identity\n\n# Use Deployments for:\n# - Stateless applications\n# - Web servers\n# - APIs\n# - Applications that can be scaled independently\n\n# StatefulSet with persistent storage\nkubectl apply -f - <<EOF\napiVersion: apps/v1\nkind: StatefulSet\nmetadata:\n  name: database\nspec:\n  serviceName: database\n  replicas: 3\n  selector:\n    matchLabels:\n      app: database\n  template:\n    metadata:\n      labels:\n        app: database\n    spec:\n      containers:\n      - name: postgres\n        image: postgres:13\n        volumeMounts:\n        - name: data\n          mountPath: /var/lib/postgresql/data\n  volumeClaimTemplates:\n  - metadata:\n      name: data\n    spec:\n      accessModes: [\"ReadWriteOnce\"]\n      storageClassName: fast-ssd\n      resources:\n        requests:\n          storage: 10Gi\nEOF',
        },
      ],
    },
    {
      title: 'Jobs and CronJobs',
      commands: [
        {
          command: 'kubectl get jobs',
          description: 'List Jobs in namespace',
          usage: 'kubectl get jobs [flags]',
          example: 'kubectl get jobs\nkubectl get jobs -A\nkubectl get jobs --watch\nkubectl get jobs -o wide\nkubectl get jobs --show-labels\nkubectl get jobs -l app=batch',
        },
        {
          command: 'kubectl create job',
          description: 'Create a Job',
          usage: 'kubectl create job <name> [flags]',
          example: 'kubectl create job my-job --image=busybox -- echo "Hello"\nkubectl create job test --image=perl -- perl -Mbignum=bpi -wle \'print bpi(2000)\'\nkubectl create job data-process --image=python:3.9 -- python process_data.py\nkubectl create job backup --image=backup-image --dry-run=client -o yaml',
        },
        {
          command: 'kubectl describe job',
          description: 'Show Job details',
          usage: 'kubectl describe job <name>',
          example: 'kubectl describe job my-job\nkubectl describe job data-process -n production --show-events',
        },
        {
          command: 'kubectl delete job',
          description: 'Delete Jobs',
          usage: 'kubectl delete job <name>',
          example: 'kubectl delete job my-job\nkubectl delete jobs --all\nkubectl delete job data-process -n production\nkubectl delete jobs -l app=batch',
        },
        {
          command: 'kubectl get cronjobs',
          description: 'List CronJobs in namespace',
          usage: 'kubectl get cronjobs [flags]',
          example: 'kubectl get cronjobs\nkubectl get cj # Short form\nkubectl get cj -A\nkubectl get cj -o wide\nkubectl get cj --show-labels\nkubectl get cj -l app=scheduled',
        },
        {
          command: 'kubectl create cronjob',
          description: 'Create a CronJob',
          usage: 'kubectl create cronjob <name> --schedule="<schedule>" [flags]',
          example: 'kubectl create cronjob my-cron --schedule="*/5 * * * *" --image=busybox -- echo "Hello"\nkubectl create cronjob backup --schedule="0 2 * * *" --image=backup-image\nkubectl create cronjob report --schedule="0 9 * * 1" --image=report-generator -- /generate-report\nkubectl create cronjob cleanup --schedule="0 3 * * *" --image=cleanup-image --dry-run=client -o yaml',
        },
        {
          command: 'kubectl describe cronjob',
          description: 'Show CronJob details',
          usage: 'kubectl describe cronjob <name>',
          example: 'kubectl describe cronjob my-cron\nkubectl describe cj backup -n production --show-events',
        },
        {
          command: 'Job types and patterns',
          description: 'Understanding different job types',
          usage: 'Job patterns and types',
          example: '# Non-parallel Job - Single completion\nkubectl apply -f - <<EOF\napiVersion: batch/v1\nkind: Job\nmetadata:\n  name: single-task\nspec:\n  template:\n    spec:\n      containers:\n      - name: worker\n        image: busybox\n        command: [\'sh\', \'-c\', \'echo "Single task completed"\']\n      restartPolicy: OnFailure\nEOF\n\n# Parallel Job with fixed completions\nkubectl apply -f - <<EOF\napiVersion: batch/v1\nkind: Job\nmetadata:\n  name: parallel-work\nspec:\n  completions: 5\n  parallelism: 2\n  template:\n    spec:\n      containers:\n      - name: worker\n        image: busybox\n        command: [\'sh\', \'-c\', \'echo "Processing item $JOB_COMPLETION_INDEX"\']\n      restartPolicy: OnFailure\nEOF\n\n# WorkQueue Job (parallelism with work queue)\nkubectl apply -f - <<EOF\napiVersion: batch/v1\nkind: Job\nmetadata:\n  name: queue-processor\nspec:\n  parallelism: 3\n  template:\n    spec:\n      containers:\n      - name: worker\n        image: queue-processor\n        env:\n        - name: WORK_QUEUE\n          value: "redis://queue:6379"\n      restartPolicy: OnFailure\nEOF',
        },
      ],
    },

    // EXPERT LEVEL
    {
      title: 'Ingress and Network Management',
      commands: [
        {
          command: 'kubectl get ingress',
          description: 'List Ingress resources in namespace',
          usage: 'kubectl get ingress [flags]',
          example: 'kubectl get ingress\nkubectl get ing # Short form\nkubectl get ing -A\nkubectl get ing -o wide\nkubectl get ing --show-labels\nkubectl get ing -l app=web',
        },
        {
          command: 'kubectl create ingress',
          description: 'Create an Ingress resource',
          usage: 'kubectl create ingress <name> [flags]',
          example: 'kubectl create ingress my-ingress --rule="host/path=service:port"\nkubectl create ingress simple --rule="foo.com/bar=svc1:8080"\nkubectl create ingress complex --rule="api.example.com/*=api-service:80" --rule="app.example.com/*=app-service:3000"\nkubectl create ingress webapp --rule="example.com/*=webapp-service:80" --dry-run=client -o yaml',
        },
        {
          command: 'kubectl describe ingress',
          description: 'Show Ingress details',
          usage: 'kubectl describe ingress <name>',
          example: 'kubectl describe ingress my-ingress\nkubectl describe ing my-ingress\nkubectl describe ingress webapp -n production --show-events',
        },
        {
          command: 'kubectl delete ingress',
          description: 'Delete an Ingress',
          usage: 'kubectl delete ingress <name>',
          example: 'kubectl delete ingress my-ingress\nkubectl delete ing webapp -n production\nkubectl delete ingress -l app=web',
        },
        {
          command: 'kubectl get networkpolicies',
          description: 'List NetworkPolicies in namespace',
          usage: 'kubectl get networkpolicies [flags]',
          example: 'kubectl get networkpolicies\nkubectl get netpol # Short form\nkubectl get netpol -A\nkubectl get netpol -o wide\nkubectl get netpol --show-labels\nkubectl get netpol -l tier=frontend',
        },
        {
          command: 'kubectl describe networkpolicy',
          description: 'Show NetworkPolicy details',
          usage: 'kubectl describe networkpolicy <name>',
          example: 'kubectl describe networkpolicy my-policy\nkubectl describe netpol deny-all\nkubectl describe networkpolicy frontend-policy -n production --show-events',
        },
        {
          command: 'Ingress controllers',
          description: 'Understanding Ingress controllers',
          usage: 'Ingress controller setup',
          example: '# NGINX Ingress Controller\nkubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.8.1/deploy/static/provider/cloud/deploy.yaml\n\n# Check Ingress controller\nkubectl get pods -n ingress-nginx\nkubectl get svc -n ingress-nginx\n\n# Test Ingress\nkubectl apply -f - <<EOF\napiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: test-ingress\n  annotations:\n    nginx.ingress.kubernetes.io/rewrite-target: /\nspec:\n  rules:\n  - host: test.example.com\n    http:\n      paths:\n      - path: /\n        pathType: Prefix\n        backend:\n          service:\n            name: test-service\n            port:\n              number: 80\nEOF\n\n# Traefik Ingress Controller\nhelm repo add traefik https://traefik.github.io/charts\nhelm install traefik traefik/traefik --namespace traefik --create-namespace',
        },
        {
          command: 'Advanced networking',
          description: 'Advanced networking concepts and tools',
          usage: 'Network troubleshooting and tools',
          example: '# Network troubleshooting\nkubectl run netshoot --image=nicolaka/netshoot -it --rm -- /bin/bash\n\n# Test DNS resolution\nnslookup kubernetes.default.svc.cluster.local\nnslookup my-service.default.svc.cluster.local\n\n# Test connectivity\nkubectl exec -it netshoot -- nc -zv my-service 80\nkubectl exec -it netshoot -- curl http://my-service.default.svc.cluster.local\n\n# Check network policies\nkubectl get networkpolicy -o yaml\nkubectl describe networkpolicy\n\n# Service mesh with Istio\nkubectl apply -f https://raw.githubusercontent.com/istio/istio/release-1.18/manifests/charts/base/istio-1.18.0.yaml\n\n# Check service mesh\nkubectl get pods -n istio-system\nkubectl get svc -n istio-system',
        },
      ],
    },
    {
      title: 'RBAC and Security',
      commands: [
        {
          command: 'kubectl get roles',
          description: 'List Roles in namespace',
          usage: 'kubectl get roles [flags]',
          example: 'kubectl get roles\nkubectl get roles -A\nkubectl get roles -n <namespace>\nkubectl get roles -o wide\nkubectl get roles --show-labels\nkubectl get roles -l app=web',
        },
        {
          command: 'kubectl create role',
          description: 'Create a Role with specific permissions',
          usage: 'kubectl create role <name> --verb=<verbs> --resource=<resources>',
          example: 'kubectl create role pod-reader --verb=get,list,watch --resource=pods\nkubectl create role deployment-manager --verb=* --resource=deployments\nkubectl create role config-editor --verb=get,update,patch --resource=configmaps\nkubectl create role secret-viewer --verb=get,list --resource=secrets',
        },
        {
          command: 'kubectl get rolebindings',
          description: 'List RoleBindings in namespace',
          usage: 'kubectl get rolebindings [flags]',
          example: 'kubectl get rolebindings\nkubectl get rolebindings -A\nkubectl get rolebindings -n <namespace>\nkubectl get rolebindings -o wide\nkubectl get rolebindings --show-labels',
        },
        {
          command: 'kubectl create rolebinding',
          description: 'Create a RoleBinding',
          usage: 'kubectl create rolebinding <name> --role=<role> --user=<user>',
          example: 'kubectl create rolebinding my-binding --role=pod-reader --user=jane\nkubectl create rolebinding admin-binding --clusterrole=admin --user=john\nkubectl create rolebinding dev-binding --role=developer --serviceaccount=default:dev-sa\nkubectl create rolebinding readonly-binding --role=pod-reader --group=developers',
        },
        {
          command: 'kubectl get clusterroles',
          description: 'List ClusterRoles in cluster',
          usage: 'kubectl get clusterroles [flags]',
          example: 'kubectl get clusterroles\nkubectl get clusterroles --show-labels\nkubectl get clusterroles -o wide\nkubectl get clusterroles -l app=system\nkubectl get clusterroles | grep -E "(admin|edit|view)"',
        },
        {
          command: 'kubectl create clusterrole',
          description: 'Create a ClusterRole with cluster-wide permissions',
          usage: 'kubectl create clusterrole <name> [flags]',
          example: 'kubectl create clusterrole pod-reader --verb=get,list,watch --resource=pods\nkubectl create clusterrole secret-reader --verb=get --resource=secrets\nkubectl create clusterrole node-admin --verb=* --resource=nodes\nkubectl create clusterrole namespace-admin --verb=* --resource=namespaces',
        },
        {
          command: 'kubectl get clusterrolebindings',
          description: 'List ClusterRoleBindings in cluster',
          usage: 'kubectl get clusterrolebindings [flags]',
          example: 'kubectl get clusterrolebindings\nkubectl get clusterrolebindings -o wide\nkubectl get clusterrolebindings --show-labels\nkubectl get clusterrolebindings -l app=system',
        },
        {
          command: 'kubectl create clusterrolebinding',
          description: 'Create a ClusterRoleBinding',
          usage: 'kubectl create clusterrolebinding <name> [flags]',
          example: 'kubectl create clusterrolebinding my-binding --clusterrole=cluster-admin --user=admin\nkubectl create clusterrolebinding read-secrets --clusterrole=secret-reader --serviceaccount=default:my-sa\nkubectl create clusterrolebinding node-admin --clusterrole=node-admin --user=ops-user\nkubectl create clusterrolebinding developers --clusterrole=developer --group=developers',
        },
        {
          command: 'kubectl auth can-i',
          description: 'Check user permissions for specific actions',
          usage: 'kubectl auth can-i <verb> <resource>',
          example: 'kubectl auth can-i create pods\nkubectl auth can-i delete deployments\nkubectl auth can-i \'*\' \'*\' --all-namespaces\nkubectl auth can-i list secrets --as=jane\nkubectl auth can-i create deployments --as=system:serviceaccount:default:my-sa\nkubectl auth can-i get nodes --as=ops-user',
        },
        {
          command: 'kubectl create serviceaccount',
          description: 'Create a ServiceAccount',
          usage: 'kubectl create serviceaccount <name>',
          example: 'kubectl create serviceaccount my-sa\nkubectl create sa app-sa # Short form\nkubectl create serviceaccount pipeline-sa -n production\nkubectl create serviceaccount readonly-sa --dry-run=client -o yaml',
        },
        {
          command: 'kubectl get serviceaccounts',
          description: 'List ServiceAccounts in namespace',
          usage: 'kubectl get serviceaccounts [flags]',
          example: 'kubectl get serviceaccounts\nkubectl get sa # Short form\nkubectl get sa -A\nkubectl get sa -o wide\nkubectl get sa --show-labels\nkubectl get sa -l app=web',
        },
        {
          command: 'Security best practices',
          description: 'Implementing security best practices',
          usage: 'Security implementation patterns',
          example: '# Principle of least privilege\nkubectl create role readonly-role --verb=get,list,watch --resource=pods,services,configmaps\nkubectl create rolebinding readonly-binding --role=readonly-role --user=readonly-user\n\n# Service accounts for applications\nkubectl create serviceaccount app-sa\nkubectl create role app-role --verb=get,list,watch,create,update,patch,delete --resource=pods,configmaps\nkubectl create rolebinding app-binding --role=app-role --serviceaccount=default:app-sa\n\n# Network policies for security\nkubectl apply -f - <<EOF\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: deny-all\nspec:\n  podSelector: {}\n  policyTypes:\n  - Ingress\n  - Egress\nEOF\n\n# Pod Security Standards\nkubectl label namespace default pod-security.kubernetes.io/enforce=baseline\nkubectl label namespace production pod-security.kubernetes.io/enforce=restricted\n\n# Security Context\nkubectl apply -f - <<EOF\napiVersion: v1\nkind: Pod\nmetadata:\n  name: secure-pod\nspec:\n  securityContext:\n    runAsNonRoot: true\n    runAsUser: 1000\n    fsGroup: 2000\n  containers:\n  - name: app\n    image: nginx\n    securityContext:\n      allowPrivilegeEscalation: false\n      readOnlyRootFilesystem: true\n      capabilities:\n        drop:\n        - ALL\nEOF',
        },
      ],
    },
    {
      title: 'Resource Management and Quotas',
      commands: [
        {
          command: 'kubectl get resourcequotas',
          description: 'List ResourceQuotas in namespace',
          usage: 'kubectl get resourcequotas [flags]',
          example: 'kubectl get resourcequotas\nkubectl get quota # Short form\nkubectl get quota -A\nkubectl get quota -o wide\nkubectl get quota --show-labels\nkubectl get quota -l environment=production',
        },
        {
          command: 'kubectl describe resourcequota',
          description: 'Show ResourceQuota details',
          usage: 'kubectl describe resourcequota <name>',
          example: 'kubectl describe resourcequota compute-quota\nkubectl describe quota memory-quota -n production --show-events',
        },
        {
          command: 'kubectl get limitranges',
          description: 'List LimitRanges in namespace',
          usage: 'kubectl get limitranges [flags]',
          example: 'kubectl get limitranges\nkubectl get limits # Short form\nkubectl get limits -A\nkubectl get limits -o wide\nkubectl get limits --show-labels',
        },
        {
          command: 'kubectl describe limitrange',
          description: 'Show LimitRange details',
          usage: 'kubectl describe limitrange <name>',
          example: 'kubectl describe limitrange default-limits\nkubectl describe limits cpu-limits -n production --show-events',
        },
        {
          command: 'kubectl get hpa',
          description: 'List HorizontalPodAutoscalers in namespace',
          usage: 'kubectl get hpa [flags]',
          example: 'kubectl get hpa\nkubectl get horizontalpodautoscalers\nkubectl get hpa -A\nkubectl get hpa -o wide\nkubectl get hpa --show-labels\nkubectl get hpa -l app=web',
        },
        {
          command: 'kubectl describe hpa',
          description: 'Show HPA details',
          usage: 'kubectl describe hpa <name>',
          example: 'kubectl describe hpa webapp-hpa\nkubectl describe horizontalpodautoscaler api-hpa -n production --show-events',
        },
        {
          command: 'kubectl autoscale (HPA)',
          description: 'Create HorizontalPodAutoscaler',
          usage: 'kubectl autoscale <resource> <name> [flags]',
          example: 'kubectl autoscale deployment webapp --min=2 --max=10 --cpu-percent=80\nkubectl autoscale deployment api --min=1 --max=5 --cpu-percent=70\nkubectl autoscale statefulset database --min=1 --max=3 --cpu-percent=60\nkubectl autoscale deployment frontend --min=2 --max=8 --cpu-percent=75 -n production',
        },
        {
          command: 'Resource quotas setup',
          description: 'Setting up resource quotas and limits',
          usage: 'Resource management implementation',
          example: '# Create ResourceQuota\nkubectl apply -f - <<EOF\napiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: compute-quota\n  namespace: production\nspec:\n  hard:\n    requests.cpu: "10"\n    requests.memory: 20Gi\n    limits.cpu: "20"\n    limits.memory: 40Gi\n    persistentvolumeclaims: "10"\n    pods: "20"\n    services: "10"\n    secrets: "10"\n    configmaps: "10"\nEOF\n\n# Create LimitRange\nkubectl apply -f - <<EOF\napiVersion: v1\nkind: LimitRange\nmetadata:\n  name: default-limits\n  namespace: production\nspec:\n  limits:\n  - default:\n      cpu: 500m\n      memory: 512Mi\n    defaultRequest:\n      cpu: 100m\n      memory: 128Mi\n    type: Container\n  - max:\n      cpu: "2"\n      memory: 4Gi\n    min:\n      cpu: 50m\n      memory: 64Mi\n    type: Container\n  - max:\n      storage: 10Gi\n    min:\n      storage: 1Gi\n    type: PersistentVolumeClaim\nEOF\n\n# Create PriorityClass\nkubectl apply -f - <<EOF\napiVersion: scheduling.k8s.io/v1\nkind: PriorityClass\nmetadata:\n  name: high-priority\nvalue: 1000000\nglobalDefault: false\ndescription: "High priority class"\nEOF',
        },
      ],
    },
    {
      title: 'Advanced Operations and Troubleshooting',
      commands: [
        {
          command: 'kubectl get events',
          description: 'List cluster events for troubleshooting',
          usage: 'kubectl get events [flags]',
          example: 'kubectl get events\nkubectl get events --sort-by=.metadata.creationTimestamp\nkubectl get events --field-selector type=Warning\nkubectl get events -A\nkubectl get events --watch\nkubectl get events --field-selector involvedObject.name=my-pod\nkubectl get events --field-selector involvedObject.kind=Pod',
        },
        {
          command: 'kubectl debug',
          description: 'Debug pod and node issues',
          usage: 'kubectl debug <resource> [flags]',
          example: 'kubectl debug my-pod -it --image=busybox\nkubectl debug my-pod -it --copy-to=my-pod-debug\nkubectl debug node/worker-1 -it --image=ubuntu\nkubectl debug my-pod --share-processes --copy-to=debug-pod\nkubectl debug deployment/webapp --image=debug-tools --copy-to=webapp-debug',
        },
        {
          command: 'kubectl cp',
          description: 'Copy files to/from containers',
          usage: 'kubectl cp <source> <dest> [flags]',
          example: 'kubectl cp my-pod:/tmp/file.txt ./file.txt\nkubectl cp ./file.txt my-pod:/tmp/\nkubectl cp my-pod:/logs/ ./logs/ -c <container>\nkubectl cp ./config/ my-pod:/etc/config/ -c config-container\nkubectl cp my-pod:/app/data.tar.gz - | tar -xzf -',
        },
        {
          command: 'kubectl attach',
          description: 'Attach to running container',
          usage: 'kubectl attach <pod> [flags]',
          example: 'kubectl attach my-pod -it\nkubectl attach my-pod -c container-name\nkubectl attach my-pod -it -c main-container\nkubectl attach my-pod --stdin=false',
        },
        {
          command: 'kubectl wait',
          description: 'Wait for specific conditions',
          usage: 'kubectl wait <resource> <name> --for=<condition>',
          example: 'kubectl wait --for=condition=ready pod/my-pod\nkubectl wait --for=condition=available deployment/nginx\nkubectl wait --for=delete pod/my-pod --timeout=60s\nkubectl wait --for=condition=complete job/my-job\nkubectl wait --for=condition=established service/webapp',
        },
        {
          command: 'kubectl diff',
          description: 'Show diff of changes to be applied',
          usage: 'kubectl diff -f <file>',
          example: 'kubectl diff -f deployment.yaml\nkubectl diff -f configs/\nkubectl diff -f https://example.com/manifest.yaml\nkubectl diff -k ./kustomization',
        },
        {
          command: 'kubectl apply (last-applied)',
          description: 'Apply with last-applied configuration tracking',
          usage: 'kubectl apply -f <file> [flags]',
          example: 'kubectl apply -f deployment.yaml --record\nkubectl apply -f configs/ --recursive\nkubectl apply -f - --filename=- # stdin\nkubectl apply -k ./kustomization --server-side\nkubectl apply -f deployment.yaml --prune -l app=webapp',
        },
        {
          command: 'kubectl replace',
          description: 'Replace resource from file',
          usage: 'kubectl replace -f <file>',
          example: 'kubectl replace -f deployment.yaml\nkubectl replace --force -f pod.yaml\nkubectl replace -f service.yaml --force\nkubectl replace -f configmap.yaml --save-config',
        },
        {
          command: 'kubectl patch',
          description: 'Update resource fields strategically',
          usage: 'kubectl patch <resource> <name> -p <patch>',
          example: 'kubectl patch deployment nginx -p \'{"spec":{"replicas":3}}\'\nkubectl patch pod my-pod -p \'{"metadata":{"labels":{"app":"v2"}}}\'\nkubectl patch svc nginx --type=json -p=\'[{"op": "replace", "path": "/spec/type", "value":"NodePort"}]\'\nkubectl patch deployment webapp --type=\'merge\' -p=\'{"spec":{"template":{"spec":{"containers":[{"name":"webapp","image":"nginx:1.27"}]}}}}\'',
        },
        {
          command: 'Troubleshooting workflow',
          description: 'Systematic troubleshooting approach',
          usage: 'Troubleshooting methodology',
          example: '# 1. Check cluster status\nkubectl get nodes\nkubectl get componentstatuses\nkubectl cluster-info\n\n# 2. Check resource status\nkubectl get all -A\nkubectl get events --sort-by=.metadata.creationTimestamp\n\n# 3. Investigate specific issues\nkubectl describe pod <failing-pod>\nkubectl logs <failing-pod> --previous\nkubectl get events --field-selector involvedObject.name=<failing-pod>\n\n# 4. Debug with ephemeral containers\nkubectl debug <failing-pod> -it --image=busybox --share-processes\n\n# 5. Check network connectivity\nkubectl run network-test --image=nicolaka/netshoot -it --rm -- /bin/bash\n\n# 6. Verify RBAC permissions\nkubectl auth can-i --list --as=<user>\nkubectl auth can-i create pods --as=<serviceaccount>\n\n# 7. Check resource usage\nkubectl top nodes\nkubectl top pods\nkubectl describe node <node-name>\n\n# 8. Validate configuration\nkubectl diff -f <manifest.yaml>\nkubectl apply --dry-run=client -f <manifest.yaml>',
        },
      ],
    },
    {
      title: 'Custom Resources and Extensions',
      commands: [
        {
          command: 'kubectl api-resources',
          description: 'List all available API resources',
          usage: 'kubectl api-resources [flags]',
          example: 'kubectl api-resources\nkubectl api-resources --namespaced=true\nkubectl api-resources --api-group=apps\nkubectl api-resources --verbs=list,get\nkubectl api-resources -o wide\nkubectl api-resources --sort-by=name',
        },
        {
          command: 'kubectl api-versions',
          description: 'List available API versions',
          usage: 'kubectl api-versions',
          example: 'kubectl api-versions\nkubectl api-versions | grep -E "(apps|extensions|networking)"\nkubectl api-versions | sort',
        },
        {
          command: 'kubectl get crd',
          description: 'List Custom Resource Definitions',
          usage: 'kubectl get crd [flags]',
          example: 'kubectl get crd\nkubectl get customresourcedefinitions\nkubectl get crd -A\nkubectl get crd -o wide\nkubectl get crd --show-labels\nkubectl get crd | grep -E "(cert|prometheus|argocd)"',
        },
        {
          command: 'kubectl describe crd',
          description: 'Show CRD details',
          usage: 'kubectl describe crd <name>',
          example: 'kubectl describe crd certificates.cert-manager.io\nkubectl describe crd prometheuses.monitoring.coreos.com\nkubectl describe crd applications.argoproj.io --show-events',
        },
        {
          command: 'kubectl explain (custom)',
          description: 'Get documentation for custom resources',
          usage: 'kubectl explain <custom-resource>[.field]',
          example: 'kubectl explain certificate.cert-manager.io\nkubectl explain prometheus.monitoring.coreos.io.spec\nkubectl explain application.argoproj.io.spec.source\nkubectl explain elasticsearches.elasticsearch.k8s.elastic.co',
        },
        {
          command: 'kubectl get (custom resources)',
          description: 'List instances of custom resources',
          usage: 'kubectl get <custom-resource-type> [flags]',
          example: 'kubectl get certificates\nkubectl get prometheuses\nkubectl get applications\nkubectl get elasticsearches\nkubectl get virtualservices.networking.istio.io\nkubectl get gateways.networking.istio.io\nkubectl get argocds.argoproj.io',
        },
        {
          command: 'Custom resource examples',
          description: 'Working with popular custom resources',
          usage: 'Custom resource management',
          example: '# Cert-Manager certificates\nkubectl apply -f - <<EOF\napiVersion: cert-manager.io/v1\nkind: Certificate\nmetadata:\n  name: example-com-tls\n  namespace: default\nspec:\n  secretName: example-com-tls\n  issuerRef:\n    name: letsencrypt-prod\n    kind: ClusterIssuer\n  dnsNames:\n  - example.com\n  - www.example.com\nEOF\n\n# Prometheus monitoring\nkubectl apply -f - <<EOF\napiVersion: monitoring.coreos.com/v1\nkind: Prometheus\nmetadata:\n  name: prometheus\nspec:\n  serviceAccountName: prometheus\n  serviceMonitorSelector:\n    matchLabels:\n      team: frontend\n  resources:\n    requests:\n      memory: 400Mi\n      cpu: 100m\n    limits:\n      memory: 2Gi\n      cpu: 1000m\nEOF\n\n# ArgoCD applications\nkubectl apply -f - <<EOF\napiVersion: argoproj.io/v1alpha1\nkind: Application\nmetadata:\n  name: webapp-prod\n  namespace: argocd\nspec:\n  project: default\n  source:\n    repoURL: https://github.com/example/webapp.git\n    targetRevision: HEAD\n    path: manifests\n  destination:\n    server: https://kubernetes.default.svc\n    namespace: production\n  syncPolicy:\n    automated:\n      prune: true\n      selfHeal: true\nEOF',
        },
      ],
    },
    {
      title: 'Performance Monitoring and Optimization',
      commands: [
        {
          command: 'kubectl top nodes',
          description: 'Show node resource usage metrics',
          usage: 'kubectl top nodes [flags]',
          example: 'kubectl top nodes\nkubectl top nodes --sort-by=cpu\nkubectl top nodes --sort-by=memory\nkubectl top nodes --no-headers\nkubectl top nodes --use-protocol-buffers\nkubectl top nodes -l environment=production',
        },
        {
          command: 'kubectl top pods',
          description: 'Show pod resource usage metrics',
          usage: 'kubectl top pods [flags]',
          example: 'kubectl top pods\nkubectl top pods -A\nkubectl top pods --containers\nkubectl top pods --sort-by=memory\nkubectl top pod my-pod\nkubectl top pods -l app=webapp\nkubectl top pods --use-protocol-buffers',
        },
        {
          command: 'kubectl get metrics',
          description: 'Get metrics server information',
          usage: 'kubectl get --raw /apis/metrics.k8s.io/v1beta1',
          example: 'kubectl get --raw "/apis/metrics.k8s.io/v1beta1/nodes"\nkubectl get --raw "/apis/metrics.k8s.io/v1beta1/pods"\nkubectl get --raw "/apis/metrics.k8s.io/v1beta1/nodes" | jq .\nkubectl get --raw "/apis/metrics.k8s.io/v1beta1/pods" | jq .',
        },
        {
          command: 'Performance profiling',
          description: 'Container and application performance profiling',
          usage: 'Performance analysis tools',
          example: '# Install metrics server\nkubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml\n\n# Check metrics server\nkubectl get pods -n kube-system -l k8s-app=metrics-server\nkubectl get apiservice v1beta1.metrics.k8s.io\n\n# Performance profiling with perf\nkubectl debug node/worker-1 --image=perf --profile --privileged -- sh -c "perf record -g -a sleep 30; perf report"\n\n# Container profiling\nkubectl exec -it my-pod -- /bin/sh -c "apt-get update && apt-get install -y perf"\nkubectl exec -it my-pod -- perf record -g -p 1\n\n# Java application profiling\nkubectl exec -it java-pod -- jcmd 1 VM.native_memory summary\nkubectl exec -it java-pod -- jstack 1\n\n# Node.js profiling\nkubectl port-forward node-pod 9229:9229\n# Connect Chrome DevTools to localhost:9229',
        },
        {
          command: 'Resource optimization',
          description: 'Optimize resource allocation and usage',
          usage: 'Resource optimization strategies',
          example: '# Resource requests and limits optimization\nkubectl apply -f - <<EOF\napiVersion: v1\nkind: Pod\nmetadata:\n  name: optimized-pod\nspec:\n  containers:\n  - name: app\n    image: nginx\n    resources:\n      requests:\n        cpu: 100m\n        memory: 128Mi\n      limits:\n        cpu: 500m\n        memory: 512Mi\n    env:\n    - name: GOGC\n      value: "100"\n    - name: GOMEMLIMIT\n      value: "400MiB"\nEOF\n\n# Vertical Pod Autoscaler\nkubectl apply -f - <<EOF\napiVersion: autoscaling.k8s.io/v1\nkind: VerticalPodAutoscaler\nmetadata:\n  name: webapp-vpa\nspec:\n  targetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: webapp\n  updatePolicy:\n    updateMode: "Auto"\n  resourcePolicy:\n    containerPolicies:\n    - containerName: webapp\n      maxAllowed:\n        cpu: 2\n        memory: 4Gi\n      minAllowed:\n        cpu: 100m\n        memory: 128Mi\nEOF\n\n# Cluster Autoscaler\nhelm repo add autoscaler https://kubernetes.github.io/autoscaler\nhelm install cluster-autoscaler autoscaler/cluster-autoscaler \\\n  --namespace kube-system \\\n  --set autoDiscovery.clusterName=my-cluster \\\n  --set awsRegion=us-west-2 \\\n  --set scaleDownUnneededTime=10m',
        },
      ],
    },
    {
      title: 'Backup and Disaster Recovery',
      commands: [
        {
          command: 'etcd backup',
          description: 'Backup etcd cluster data',
          usage: 'ETCDCTL_API=3 etcdctl snapshot save <file>',
          example: '# Static pod backup\nETCDCTL_API=3 etcdctl snapshot save /tmp/etcd-backup.db \\\n  --endpoints=https://127.0.0.1:2379 \\\n  --cacert=/etc/kubernetes/pki/etcd/ca.crt \\\n  --cert=/etc/kubernetes/pki/etcd/server.crt \\\n  --key=/etc/kubernetes/pki/etcd/server.key\n\n# External etcd backup\nETCDCTL_API=3 etcdctl snapshot save /backup/etcd-$(date +%Y%m%d).db \\\n  --endpoints=https://etcd-1:2379,https://etcd-2:2379,https://etcd-3:2379 \\\n  --cacert=/etc/kubernetes/pki/etcd/ca.crt \\\n  --cert=/etc/kubernetes/pki/etcd/admin.crt \\\n  --key=/etc/kubernetes/pki/etcd/admin.key',
        },
        {
          command: 'etcd restore',
          description: 'Restore etcd from backup',
          usage: 'etcdctl snapshot restore <file> [flags]',
          example: '# Restore etcd from backup\netcdctl snapshot restore /tmp/etcd-backup.db \\\n  --data-dir /var/lib/etcd-restore \\\n  --initial-cluster etcd-1=https://etcd-1:2380,etcd-2=https://etcd-2:2380,etcd-3=https://etcd-3:2380 \\\n  --initial-cluster-token etcd-cluster \\\n  --initial-advertise-peer-urls https://etcd-1:2380 \\\n  --name etcd-1 \\\n  --cert=/etc/kubernetes/pki/etcd/server.crt \\\n  --key=/etc/kubernetes/pki/etcd/server.key \\\n  --trusted-ca-file=/etc/kubernetes/pki/etcd/ca.crt',
        },
        {
          command: 'Velero backup',
          description: 'Backup with Velero for disaster recovery',
          usage: 'velero backup create <name> [flags]',
          example: '# Install Velero\nvelero install --provider aws --bucket velero-backups \\\n  --secret-file ./credentials-velero \\\n  --use-restic --default-volumes-to-restic\n\n# Create backup\nvelero backup create full-cluster-backup --from-cluster\nvelero backup create namespace-backup --include-namespaces production\nvelero backup create selective-backup --include-resources deployments,configmaps,secrets\n\n# Schedule backups\nvelero schedule create daily-backup --schedule="0 2 * * *" --ttl 720h0m0s\nvelero schedule create weekly-backup --schedule="0 2 * * 0" --ttl 168h0m0s\n\n# Restore from backup\nvelero restore create --from-backup full-cluster-backup\nvelero restore create --from-backup namespace-backup --include-namespaces production\nvelero restore create selective-restore --from-backup selective-backup --include-resources deployments',
        },
        {
          command: 'Application backup',
          description: 'Application-level backup strategies',
          usage: 'Application backup implementation',
          example: '# Database backup job\nkubectl apply -f - <<EOF\napiVersion: batch/v1\nkind: CronJob\nmetadata:\n  name: database-backup\nspec:\n  schedule: "0 2 * * *"\n  jobTemplate:\n    spec:\n      template:\n        spec:\n          containers:\n          - name: postgres-backup\n            image: postgres:13\n            command:\n            - /bin/bash\n            - -c\n            - |\n              pg_dump -h \$POSTGRES_HOST -U \$POSTGRES_USER \$POSTGRES_DB > /backup/backup-$(date +%Y%m%d).sql\n              aws s3 cp /backup/backup-$(date +%Y%m%d).sql s3://backups/database/\n            env:\n            - name: POSTGRES_HOST\n              value: postgres\n            - name: POSTGRES_USER\n              valueFrom:\n                secretKeyRef:\n                  name: postgres-secret\n                  key: username\n            - name: PGPASSWORD\n              valueFrom:\n                secretKeyRef:\n                  name: postgres-secret\n                  key: password\n            volumeMounts:\n            - name: backup-storage\n              mountPath: /backup\n          volumes:\n          - name: backup-storage\n            persistentVolumeClaim:\n              claimName: backup-pvc\n          restartPolicy: OnFailure\nEOF\n\n# File system backup\nkubectl apply -f - <<EOF\napiVersion: batch/v1\nkind: Job\nmetadata:\n  name: fs-backup\nspec:\n  template:\n    spec:\n      containers:\n      - name: backup\n        image: alpine:3.18\n        command:\n        - /bin/sh\n        - -c\n        - |\n          tar -czf /backup/app-data-$(date +%Y%m%d).tar.gz /app/data\n          aws s3 cp /backup/app-data-$(date +%Y%m%d).tar.gz s3://backups/app/\n        volumeMounts:\n        - name: app-data\n          mountPath: /app/data\n          readOnly: true\n        - name: backup-storage\n          mountPath: /backup\n      volumes:\n      - name: app-data\n        persistentVolumeClaim:\n          claimName: app-pvc\n      - name: backup-storage\n        persistentVolumeClaim:\n          claimName: backup-pvc\n      restartPolicy: OnFailure\nEOF',
        },
      ],
    },
    {
      title: 'Multi-Cluster and Federation',
      commands: [
        {
          command: 'kubectl config view (multi-cluster)',
          description: 'Manage multiple cluster configurations',
          usage: 'kubectl config view --flatten',
          example: '# View all cluster contexts\nkubectl config get-contexts\nkubectl config view --flatten\n\n# Switch between clusters\nkubectl config use-context dev-cluster\nkubectl config use-context prod-cluster\nkubectl config use-context staging-cluster\n\n# Set namespace per context\nkubectl config set-context dev-cluster --namespace=development\nkubectl config set-context prod-cluster --namespace=production\n\n# Merge kubeconfig files\nKUBECONFIG=~/.kube/config:~/.kube/dev-config kubectl config view --flatten > ~/.kube/merged-config\nexport KUBECONFIG=~/.kube/merged-config',
        },
        {
          command: 'Cluster federation',
          description: 'Kubernetes Cluster Federation (KubeFed)',
          usage: 'kubefedctl join <cluster-name> [flags]',
          example: '# Install KubeFed\nkubectl apply -f https://github.com/kubernetes-sigs/kubefed/releases/download/v0.9.0/kubefed.yaml\n\n# Join clusters to federation\nkubefedctl join us-west-1 --cluster-context us-west-1 --host-cluster-context host-cluster\nkubefedctl join us-east-1 --cluster-context us-east-1 --host-cluster-context host-cluster\nkubefedctl join eu-west-1 --cluster-context eu-west-1 --host-cluster-context host-cluster\n\n# Create federated resources\nkubectl apply -f - <<EOF\napiVersion: types.kubefed.io/v1beta1\nkind: FederatedDeployment\nmetadata:\n  name: webapp\n  namespace: default\nspec:\n  template:\n    metadata:\n      labels:\n        app: webapp\n    spec:\n      replicas: 3\n      selector:\n        matchLabels:\n          app: webapp\n      template:\n        metadata:\n          labels:\n            app: webapp\n        spec:\n          containers:\n          - name: webapp\n            image: nginx:1.25\n            ports:\n            - containerPort: 80\n  placement:\n    clusters:\n    - name: us-west-1\n    - name: us-east-1\n    - name: eu-west-1\nEOF',
        },
        {
          command: 'Multi-cluster management tools',
          description: 'Tools for managing multiple clusters',
          usage: 'Multi-cluster tooling',
          example: '# kubectx - Switch between contexts\nkubectx\nkubectx dev-cluster\nkubectx -\n\n# kubens - Switch between namespaces\nkubens\nkubens production\nkubens -\n\n# Lens - GUI for multi-cluster management\n# Download from https://k8slens.dev/\n\n# Rancher - Multi-cluster management platform\n# Install with Docker\ndocker run -d --restart=unless-stopped -p 80:80 -p 443:443 \\\n  --privileged rancher/rancher:latest\n\n# Octant - Web-based cluster viewer\noctant --kubeconfig ~/.kube/config\n\n# k9s - Terminal UI for Kubernetes\nk9s --context dev-cluster\nk9s --context prod-cluster --namespace production',
        },
      ],
    },
    {
      title: 'Expert Tips and Best Practices',
      commands: [
        {
          command: 'kubectl aliases',
          description: 'Productivity-boosting kubectl aliases',
          usage: 'Shell aliases for kubectl',
          example: '# Common aliases\nalias k=kubectl\nalias kgp="kubectl get pods"\nalias kgs="kubectl get services"\nalias kgd="kubectl get deployments"\nalias kgsts="kubectl get statefulsets"\nalias kgds="kubectl get daemonsets"\nalias kgj="kubectl get jobs"\nalias kgcj="kubectl get cronjobs"\nalias kgn="kubectl get nodes"\nalias kga="kubectl get all"\nalias kaf="kubectl apply -f"\nalias kdf="kubectl delete -f"\nalias kdel="kubectl delete"\nalias kdes="kubectl describe"\nalias kex="kubectl exec -it"\nalias klf="kubectl logs -f"\nalias kcp="kubectl cp"\nalias kpf="kubectl port-forward"\n\n# Function aliases\nkns() { kubectl config set-context --current --namespace="$1"; }\nkc() { kubectl config use-context "$1"; }\nke() { kubectl exec -it "$1" -- sh; }\nkl() { kubectl logs -f "$1"; }\nkgpwn() { kubectl get pods -o wide --all-namespaces --sort-by=.metadata.name; }',
        },
        {
          command: 'Resource validation',
          description: 'Validate resources before applying',
          usage: 'kubectl apply --dry-run=client',
          example: '# Dry run validation\nkubectl apply --dry-run=client -f deployment.yaml\nkubectl apply --dry-run=server -f service.yaml\nkubectl create deployment test --image=nginx --dry-run=client -o yaml\n\n# Schema validation\nkubectl apply --validate=true -f configmap.yaml\nkubectl apply --validate=false -f custom-resource.yaml\n\n# Policy validation with OPA Gatekeeper\nkubectl apply -f - <<EOF\napiVersion: constraints.gatekeeper.sh/v1beta1\nkind: K8sRequiredLabels\nmetadata:\n  name: all-must-have-owner\nspec:\n  enforcementAction: deny\n  match:\n    kinds:\n      - apiGroups: [""]\n        kinds: ["Pod"]\n  parameters:\n    labels: ["owner"]\nEOF',
        },
        {
          command: 'Security hardening',
          description: 'Kubernetes security best practices',
          usage: 'Security implementation',
          example: '# Network policies\nkubectl apply -f - <<EOF\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: default-deny-all\nspec:\n  podSelector: {}\n  policyTypes:\n  - Ingress\n  - Egress\nEOF\n\n# Pod Security Standards\nkubectl label namespace production pod-security.kubernetes.io/enforce=restricted\nkubectl label namespace staging pod-security.kubernetes.io/warn=baseline\n\n# RBAC minimal permissions\nkubectl create role readonly --verb=get,list,watch --resource=pods,services,configmaps\nkubectl create rolebinding readonly-binding --role=readonly --user=readonly-user\n\n# Service accounts\nkubectl create serviceaccount app-sa\nkubectl patch deployment app -p \'{"spec":{"template":{"spec":{"serviceAccountName":"app-sa"}}}}\'\n\n# Secrets encryption\n# Enable encryption at rest in API server\n# --encryption-provider-config=/etc/kubernetes/encryption-config.yaml\n\n# Admission controllers\n# Enable PodSecurityPolicy, NodeRestriction, ResourceQuota\n# --enable-admission-plugins=PodSecurityPolicy,NodeRestriction,ResourceQuota',
        },
        {
          command: 'Performance optimization',
          description: 'Cluster and application performance tuning',
          usage: 'Performance optimization techniques',
          example: '# Resource requests and limits\nkubectl apply -f - <<EOF\napiVersion: v1\nkind: Pod\nmetadata:\n  name: optimized-pod\nspec:\n  containers:\n  - name: app\n    image: nginx\n    resources:\n      requests:\n        cpu: 100m\n        memory: 128Mi\n      limits:\n        cpu: 500m\n        memory: 512Mi\nEOF\n\n# Vertical Pod Autoscaler\nkubectl apply -f https://github.com/kubernetes/autoscaler/releases/download/vpa-v0.13.0/vpa.yaml\n\n# Cluster Autoscaler\nhelm install cluster-autoscaler autoscaler/cluster-autoscaler \\\n  --set autoDiscovery.clusterName=my-cluster \\\n  --set scaleDownUnneededTime=10m\n\n# Node tuning\n# Use appropriate instance types\n# Enable CPU pinning for performance-critical workloads\n# Configure kernel parameters\n# Use local SSD for high I/O workloads\n\n# Application optimization\n# Use appropriate base images\n# Implement health checks\n# Configure graceful shutdown\n# Use connection pooling\n# Implement caching strategies',
        },
        {
          command: 'Monitoring and observability',
          description: 'Comprehensive monitoring setup',
          usage: 'Monitoring stack implementation',
          example: '# Prometheus monitoring\nkubectl apply -f https://github.com/prometheus-operator/prometheus-operator/releases/download/v0.68.0/bundle.yaml\n\n# Grafana visualization\nhelm install grafana grafana/grafana \\\n  --namespace monitoring \\\n  --set persistence.enabled=true \\\n  --set adminPassword=admin123\n\n# Jaeger tracing\nkubectl apply -f https://github.com/jaegertracing/jaeger-operator/releases/download/v1.49.0/jaeger-operator.yaml\n\n# Loki logging\nhelm install loki grafana/loki \\\n  --namespace monitoring \\\n  --set persistence.enabled=true\n\n# Custom metrics\nkubectl apply -f - <<EOF\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: adapter-config\n  namespace: custom-metrics\ndata:\n  config.yaml: |\n    rules:\n    - seriesQuery: \'http_requests_total{namespace!="",pod!=""}\'\n      resources:\n        overrides:\n          namespace: {resource: "namespace"}\n          pod: {resource: "pod"}\n      name:\n        matches: "^_(.*)_total"\n        as: "${1}_per_second"\n      metricsQuery: \'rate(<<.Series>>{<<.LabelMatchers>>}[2m])\'\nEOF',
        },
      ],
    },
  ],
};
