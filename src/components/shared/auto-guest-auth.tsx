'use client';

/**
 * AutoGuestAuth Component
 * DISABLED - No longer needed with Supabase (guest access allowed by default)
 */
export function AutoGuestAuth() {
  // This component is disabled during Supabase migration
  // Guest users can now access content without anonymous authentication
  return null;
}
